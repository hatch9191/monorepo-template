{
  "name": "core-frontend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "next build",
    "dev": "next dev --turbopack",
    "format": "prettier --config ../../../config/.prettierrc --write --ignore-path ./.prettierignore . && eslint --cache --fix ."
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "react": "^18.3.1"
  }
}
